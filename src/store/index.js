import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

const store = new Vuex.Store({
  modules: {},
  getters: {},
  actions: {},
  state: {
    isAuthorized: false,
  },
  mutations: {},
});

export default store;
